@using FarmTrack.Models.Entities;
@{

    ViewData["Title"] = "Dashboard";
    var user = Context.Session.GetObject<User>("CurrentUser") ?? new User();
    ViewData["SessionUsername"] = user.Username;
}
<h1>Welcome, @ViewData["SessionUsername"]</h1>

 